# coding-workers-sandbox

Repositorio de pruebas del orquestador **coding-workers**. Los agentes clonan
esto dentro de un sandbox de OpenShell, arreglan lo que se les pida, y el worker
publica el resultado como PR.

Contiene un bug real con tests que lo demuestran:

```
npm test
```
